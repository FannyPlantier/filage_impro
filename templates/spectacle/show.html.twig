{% extends 'base.html.twig' %}

{% block title %}Spectacle{% endblock %}

{% block body %}
    <h1>Spectacle</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ spectacle.id }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ spectacle.date ? spectacle.date|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>NbCategorie</th>
                <td>{{ spectacle.nbCategorie }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_spectacle_index') }}">back to list</a>

    <a href="{{ path('app_spectacle_edit', {'id': spectacle.id}) }}">edit</a>

    {{ include('spectacle/_delete_form.html.twig') }}
{% endblock %}

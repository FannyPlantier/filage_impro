{% extends 'base.html.twig' %}

{% block title %}Comedien index{% endblock %}

{% block body %}
    <h1>Comedien index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Prenom</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for comedien in comediens %}
            <tr>
                <td>{{ comedien.id }}</td>
                <td>{{ comedien.prenom }}</td>
                <td>
                    <a href="{{ path('app_comedien_show', {'id': comedien.id}) }}">show</a>
                    <a href="{{ path('app_comedien_edit', {'id': comedien.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_comedien_new') }}">Create new</a>
{% endblock %}
